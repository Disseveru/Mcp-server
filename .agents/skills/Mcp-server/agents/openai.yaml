interface:
  display_name: "Mcp Server"
  short_description: "Repo-specific patterns and workflows for Mcp-server"
  default_prompt: "Use the Mcp-server repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true